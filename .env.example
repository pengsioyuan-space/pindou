# Node 24+ runtime; production must use HTTPS.
# Generate independent random values; never reuse card codes as admin secrets.
ADMIN_KEY=replace-with-at-least-20-random-characters
APP_SECRET=replace-with-at-least-32-random-characters
PORT=4173
