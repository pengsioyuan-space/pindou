# 草莓拼豆网站：功能与备份评估

## 检查范围与证据

- 来源：用户从 https://pd.luckiest.top/ 保存的 HTML 和 `_files` 目录。
- 本次检查为静态代码分析，没有执行原站脚本、提交卡密或调用原站接口。
- 保存文件共 18 个，其中 11 个为 `.js.下载` 脚本；另有无 JavaScript 扩展名的统计资源。
- 核心功能代码位于 `5123-b4d537dcca3d7699.js.下载`。React/Next.js 的编译产物及 Webpack 运行时代码也已保存，但不等于原始工程源码。
- 可重复执行 `python -X utf8 audit_saved_site.py`，生成含文件 SHA-256、引用检查和界面文字的 `inspection/saved-site-audit.json`。

## 代码确认的主要功能

| 功能 | 已观察到的实现或限制 |
| --- | --- |
| 导入 | JPG、PNG、CSV；文件选择与拖拽；图片使用 FileReader 读取 |
| 空白画板 | 首页提供 100×100 空白画板入口 |
| 网格调整 | 横向格数和颜色合并阈值；截图显示格数 10–500、阈值 0–100；其他模式和卡密状态下的上限未实测 |
| 转换模式 | 卡通（主色）、真实（平均）、抖动（误差扩散）、平滑（强化合并）、调色板映射（纯映射） |
| 图像处理 | Canvas 像素采样、颜色距离计算、调色板映射、按颜色出现次数进行合并；不需要据此推断为 AI |
| 品牌色号 | 内置颜色到 MARD、COCO、漫漫、盼盼、咪小窝的映射；代码明确提示切换色号系统只影响显示，不改变已选颜色 |
| 色板 | 品牌预设、全选/全不选、排除/恢复颜色；自定义色板 JSON 导入和导出 |
| 编辑 | 拖拽、画笔、取色、撤回、填充、矩形、镜像、颜色替换；画笔大小及矩形实心设置 |
| 去背景 | 自动方法从图案边缘统计主要背景色，再处理背景；不是已验证的语义抠图；另有手动橡皮工具 |
| 显示辅助 | 色号、坐标、颜色高亮、缩放及全屏；代码还包含框选、魔棒、描图相关状态，具体交互待运行验证 |
| 文字 | 最多 5 个字的文字拼豆图生成入口和生成逻辑 |
| 统计 | 每种颜色用量和总豆数 |
| 导出 | Canvas 生成 PNG；设置包含网格、间隔、线条颜色、坐标、色号、色号统计；可同时导出 CSV |
| CSV | 二维颜色数据；透明格使用特定标记；导入时恢复图案；不能据此认定保存全部编辑历史和工程设置 |
| 本地项目 | `1371` 脚本包含 IndexedDB 项目库和 localStorage 缓存逻辑；不能据此确认云端同步可用 |
| 商业与账号入口 | 卡密/手机号验证、绑定查询、VIP 提示、购买卡密、客服、教程等 |

页面出现“云端保存成功”等文案不是后端保存已验证的证据。账号页面、项目页面与相关路由是否完整保存尚未确认。

## 原站依赖

| 接口 | 从调用点观察到的用途 |
| --- | --- |
| `/api/frontend-config` | 读取前端文字和入口配置 |
| `/api/card-config` | 读取卡密相关配置 |
| `/api/activate-card` | 提交卡密验证 |
| `/api/query-card-binding` | 查询绑定状态 |
| `/api/upload-usage` | 导入图片时记录使用次数 |

以上相对接口在新域名下会指向新域名自身；GitHub Pages 不提供这些后端接口。不能仅复制网页就保留原站账号和卡密服务。

## 保存完整性及提交前处理

1. HTML 引用了未保存的 `polyfills-42372ed130431b0a.js.下载`。该标签带有 `nomodule`，主要面向旧浏览器，不能仅凭它缺失就认定现代浏览器必定无法运行。
2. 脚本引用 `/website_qrcode.png`、`/tuzhitu.png` 等绝对路径。部分图片可能以 `image` 文件保存，但映射和完整性尚未核实；新站尤其是 GitHub Pages 子目录部署需要处理路径。
3. 脚本扩展名为 `.js.下载`，原样部署可能导致脚本 MIME 类型不符合要求。
4. 保存的 Next.js 文档和运行时仍可能请求原路由、图片优化入口或其他数据。未做运行验证，不能声称所有资源都齐全。
5. 保存资源包含统计脚本，以及卡密服务的 `apiKey` 配置/默认值。报告不复制密钥值，是否有效也未验证；原始文件不应不经审查直接推送。原始保存件保留本地，供对照。
6. 没有取得服务端源码、数据库、原始组件源码、构建配置和依赖锁文件。只有浏览器编译产物。

## 后续实施路径

- 真正完整备份：取得原站工程和后端部署资料，然后恢复构建并核对功能；这是保留账号、卡密和云端数据行为所需的路径。
- 独立复现：保留图像处理、编辑、色板、统计和导出目标，补齐资源并建立自己的可维护工程；账号和云端功能必须明确实现，不能用假成功接口代替。
- 仅存档网页：保存静态快照及检查记录，但应明确它不是独立可用的完整站点。

当前阶段已完成保存件检查；尚未创建或推送 GitHub 仓库，尚未通过浏览器交互验证。
